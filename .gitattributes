# Text files: normalize line endings
* text=auto

# Unreal binary assets -> Git LFS.
# These are rewritten wholesale on every save, so keeping them out of
# regular git history is what stops the repo from ballooning.
*.uasset filter=lfs diff=lfs merge=lfs -text
*.umap   filter=lfs diff=lfs merge=lfs -text

# Other binary formats commonly dropped into an Unreal project
*.fbx  filter=lfs diff=lfs merge=lfs -text
*.obj  filter=lfs diff=lfs merge=lfs -text
*.png  filter=lfs diff=lfs merge=lfs -text
*.jpg  filter=lfs diff=lfs merge=lfs -text
*.tga  filter=lfs diff=lfs merge=lfs -text
*.exr  filter=lfs diff=lfs merge=lfs -text
*.wav  filter=lfs diff=lfs merge=lfs -text
*.mp3  filter=lfs diff=lfs merge=lfs -text

# Source files are text — keep them diffable
*.cpp text
*.h   text
*.cs  text
*.ini text
*.uproject text
